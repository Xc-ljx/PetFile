<mapper namespace="com.example.dream_house.mapper.CandidateMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO candidate (name, age, id_number, ..., create_time)
        VALUES (#{name}, #{age}, #{idNumber}, ..., NOW())
    </insert>

    <select id="selectByCondition" resultType="Candidate">
        SELECT * FROM candidate
        <where>
            <if test="name != null">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="phone != null">AND phone = #{phone}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>
</mapper>